# XP — Validators

Base: `https://api.ethos.network/api/v2/xp`

Validator NFT holders participate in XP validation. `validatorNftCount` on user object indicates level.

## GET `/xp/validators`

List all XP validators.

## GET `/xp/validators/profile/{userkey}`

Validator info for a specific user.
