# Profiles — Query

Base: `https://api.ethos.network/api/v2/profiles`

Note: Profile endpoints return profile-level data (distinct from full User objects in `/users`).

## POST `/profiles`

Get a paginated list of profiles.

## GET `/profiles/recent`

Get recently created profiles.

## GET `/profiles/stats`

Get profile stats (counts, etc.).
