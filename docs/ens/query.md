# ENS — Query

Base: `https://api.ethos.network/api/v2/external/ens`

## GET `/external/ens/{address}`

Get ENS details by Ethereum address.

Path: `address` (Ethereum address)

## GET `/external/ens/by-name/{name}`

Get ENS details by ENS name.

Path: `name` (ENS name, e.g. `vitalik.eth`)
