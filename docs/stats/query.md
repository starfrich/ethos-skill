# Stats — Query

Base: `https://api.ethos.network/api/v2`

## GET `/stats/score-distribution`

Get score distribution by level.

## GET `/stats/human-verification`

Get human verification statistics. Returns `summary`, `progressDistribution`, `verifiedByScoreLevel`, and `methodBreakdown`.
